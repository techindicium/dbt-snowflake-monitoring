models:
  - name: dim_snowflake_monitoring_database
    description: "Tabela de dimensão de banco de dados que fornece informações detalhadas sobre cada banco de dados."
    columns:
      - name: database_sk
        description: Chave substituta do banco de dados. Este é um identificador único gerado artificialmente usado para fins de data warehouse.
        tests:
          - not_null
          - unique
      - name: database_name
        description: Nome do banco de dados.
        
