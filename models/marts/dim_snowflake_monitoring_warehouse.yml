models:
  - name: dim_snowflake_monitoring_warehouse
    description: "Tabela de dimensão de warehouse que fornece informações detalhadas sobre cada warehouse."
    columns:
      - name: warehouse_sk
        description: Chave substituta do warehouse. Este é um identificador único gerado artificialmente usado para fins de data warehouse.
        tests:
          - not_null
          - unique
      - name: warehouse_id
        description: ID do warehouse.
      - name: warehouse_name
        description: Nome do warehouse.

